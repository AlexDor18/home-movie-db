version: '3'
services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      TMDB_API_KEY: INSERT API KEY HERE 
      DB_USER: Add username key
      DB_PASSWORD: Add Password Key
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: Add username key
      POSTGRES_PASSWORD: Add Password Key
    healthcheck:
      test: ["CMD-SHELL", "pg_isready", "-d", "db_prod"]
      interval: 30s
      timeout: 60s
      retries: 5
      start_period: 80s
