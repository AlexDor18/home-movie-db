services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      TMDB_API_KEY: INSERT API KEY HERE 
      DB_USER: homeMovieDb  
      DB_PASSWORD: homeMovieDb
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: homeMovieDb
      POSTGRES_PASSWORD: homeMovieDb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready", "-U", "homeMovieDb"]
      interval: 30s
      timeout: 60s
      retries: 5
      start_period: 80s
